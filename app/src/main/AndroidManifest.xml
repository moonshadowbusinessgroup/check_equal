<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hadimusthafa.check_equal">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Check_equal">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LogActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".calculatorActivity" />
        <activity android:name=".CheckEqualActivity" />
        <activity android:name=".CountdownTimerActivity" />
        <activity android:name=".DaysDiffActivity" />
        <activity android:name=".radiobuttonActivity2" />
        <activity android:name=".radioLogActivity" />
        <activity android:name=".ScrollingActivityOne" />
        <activity android:name=".ScrollSubmitActivity" />
    </application>

</manifest>